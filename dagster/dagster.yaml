concurrency:
  pools:
    granularity: "run"  # "op" (default: limits individual assets) or "run" (limits runs)
    default_limit: 1   # Fallback limit for unconfigured pools
    op_granularity_run_buffer: 1  # Buffer slots per run (for op granularity)